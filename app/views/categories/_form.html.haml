= form_with(model: @category, local: true) do |f|
  .form-group
    = f.label :title, class: 'control-label'
    = f.text_field :title, class: 'form-control'
    = f.submit t('links.save'), :class => "btn btn-dark my-2"


