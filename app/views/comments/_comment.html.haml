%ul.list-unstyled
  %hr
  %li.media
    .media-body.ml-3
      .d-flex.justify-content-between
        %h5.m-1
          = comment.commenter
        - unless comment.id.nil?
          .date #{time_ago_in_words(comment.created_at)} #{t('images.show.time')}
      %p
        = comment.body